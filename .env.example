# Runner mode: off | optional | required
LANGFUSE_MODE=optional
LANGFUSE_BASE_URL=http://localhost:3000
LANGFUSE_PUBLIC_KEY=pk-lf-local
LANGFUSE_SECRET_KEY=sk-lf-local

# Local model invocation
CODEX_BIN=codex
CODEX_MODEL=
CASE_TIMEOUT_MS=300000

# Never send dataset expected outputs to the agent under test.
EXPERIMENT_PREFIX=local-skill-agent
